// Import global CSS for TailwindCSS to work
import '../app.css';

// This file is the entry point for SvelteKit.
// SvelteKit uses a different approach than standard Svelte applications
// and doesn't need explicit initialization in the source code.
export const prerender = true;
